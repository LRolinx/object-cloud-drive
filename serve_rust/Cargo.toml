[package]
name = "serve_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
sqlx = {version = "0.8",features = ["runtime-tokio-native-tls","sqlite"]}
tokio = {version = "1",features = ["full"]}
